// export function sendTurn() {
//     // We have a move and target so we can send to server now
//     const payload: ClientPayload = {
//       lobbyCode: lobby._id,
//       user: currentPlayer.name,
//       move: currentPlayer.turn.move,
//       target: action.payload.target,
//     }

// }
